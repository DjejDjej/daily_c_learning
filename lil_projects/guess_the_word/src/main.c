#include "file_handler.h"
#include "game.h"
#include "input_handler.h"
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
  // char *input;
  int status;
  // bool sentence;
  // char *path = "sentences";
  // char *mode = "r";
  // struct file_handle *fh = open_file(path, mode, &status);
  // struct sentences *sent = get_sentences_from_fh(fh, &status);
  // int x = get_rnd_sentence_index(sent);
  // char *sentence = obfuscate_sentence(sent, x);
  //
  // int hit = char_guessed('a', sentence, sent, x);
init_game();


  // int wheel[] = {0, 100, 100, 200, 300, 400, 500};
  // int wheel_size = (sizeof(wheel) / sizeof(wheel[0])) -1 ;
  // int x = spin_wheel(wheel, wheel_size);

  // for (size_t i = 0; i < sent->count; i++) {
  // printf("%s\n",sent->data[i]);
  //
  // }
  // free(sentence);
  // fh_close(&fh);
  // sentence_free(sent);
  return 0;
}
